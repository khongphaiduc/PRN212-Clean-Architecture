<Window x:Class="RetailPresentation.ImportStockWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Nhập Kho Sản Phẩm" Height="300" Width="400"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Sản phẩm:" Grid.Row="0" Grid.Column="0" Margin="0,5,10,15"/>
        <TextBlock x:Name="lblProductName" Grid.Row="0" Grid.Column="1" FontWeight="Bold" Margin="0,5,0,15"/>

        <TextBlock Text="Tồn kho hiện tại:" Grid.Row="1" Grid.Column="0" Margin="0,0,10,15"/>
        <TextBlock x:Name="lblCurrentQuantity" Grid.Row="1" Grid.Column="1" FontWeight="Bold" Margin="0,0,0,15"/>

        <TextBlock Text="Số lượng nhập:" Grid.Row="2" Grid.Column="0" Margin="0,0,10,15" VerticalAlignment="Center"/>
        <TextBox x:Name="txtImportQuantity" Grid.Row="2" Grid.Column="1" Margin="0,0,0,15" Height="25"/>

        <TextBlock Text="Ghi chú:" Grid.Row="3" Grid.Column="0" Margin="0,0,10,15" VerticalAlignment="Top"/>
        <TextBox x:Name="txtNote" Grid.Row="3" Grid.Column="1" Margin="0,0,0,15" Height="50" TextWrapping="Wrap" AcceptsReturn="True"/>

        <StackPanel Grid.Row="4" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">
            <Button x:Name="btnImport" Content="Import" Width="80" Margin="0,0,10,0" Click="btnImport_Click"/>
            <Button x:Name="btnCancel" Content="Cancel" Width="80" Click="btnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>