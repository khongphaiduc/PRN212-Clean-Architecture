<Window x:Class="RetailPresentation.ImportStockWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Nhập kho sản phẩm" Height="440" Width="520"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="24">
        <Border Background="{StaticResource SurfaceBrush}"
                BorderBrush="{StaticResource BorderBrushSoft}"
                BorderThickness="1"
                CornerRadius="8"
                Padding="24">
            <Border.Effect>
                <DropShadowEffect BlurRadius="18" ShadowDepth="2" Opacity="0.08" />
            </Border.Effect>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Margin="0,0,0,20">
                    <TextBlock Text="Nhập kho" Style="{StaticResource PageTitleText}" />
                    <TextBlock Text="Cập nhật số lượng tồn và ghi nhận giao dịch nhập hàng"
                               Foreground="{StaticResource MutedTextBrush}"
                               FontSize="14"
                               Margin="0,6,0,0" />
                </StackPanel>

                <Border Grid.Row="1"
                        Background="#F8FAFC"
                        BorderBrush="{StaticResource BorderBrushSoft}"
                        BorderThickness="1"
                        CornerRadius="8"
                        Padding="16"
                        Margin="0,0,0,18">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="Sản phẩm" Style="{StaticResource LabelText}" />
                            <TextBlock x:Name="lblProductName"
                                       FontSize="18"
                                       FontWeight="SemiBold"
                                       Margin="0,4,0,0" />
                        </StackPanel>
                        <StackPanel Grid.Column="1" HorizontalAlignment="Right">
                            <TextBlock Text="Tồn kho hiện tại" Style="{StaticResource LabelText}" />
                            <TextBlock x:Name="lblCurrentQuantity"
                                       FontSize="22"
                                       FontWeight="SemiBold"
                                       HorizontalAlignment="Right"
                                       Foreground="{StaticResource AccentBrush}"
                                       Margin="0,2,0,0" />
                        </StackPanel>
                    </Grid>
                </Border>

                <Grid Grid.Row="2">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Margin="0,0,0,16">
                        <TextBlock Text="Số lượng nhập" Style="{StaticResource LabelText}" Margin="0,0,0,6" />
                        <TextBox x:Name="txtImportQuantity" />
                    </StackPanel>

                    <StackPanel Grid.Row="1">
                        <TextBlock Text="Ghi chú" Style="{StaticResource LabelText}" Margin="0,0,0,6" />
                        <TextBox x:Name="txtNote"
                                 Height="78"
                                 TextWrapping="Wrap"
                                 AcceptsReturn="True"
                                 VerticalContentAlignment="Top" />
                    </StackPanel>
                </Grid>

                <StackPanel Grid.Row="3"
                            Orientation="Horizontal"
                            HorizontalAlignment="Right"
                            Margin="0,20,0,0">
                    <Button x:Name="btnCancel"
                            Content="Hủy"
                            Style="{StaticResource SecondaryButton}"
                            Margin="0,0,10,0"
                            Click="btnCancel_Click" />
                    <Button x:Name="btnImport"
                            Content="Xác nhận nhập"
                            Style="{StaticResource SuccessButton}"
                            Click="btnImport_Click" />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
