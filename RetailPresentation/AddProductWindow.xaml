<Window x:Class="RetailPresentation.AddProductWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Thêm Sản Phẩm Mới" Height="300" Width="400"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Tên sản phẩm:" Grid.Row="0" Grid.Column="0" Margin="0,5,10,15" VerticalAlignment="Center"/>
        <TextBox x:Name="txtName" Grid.Row="0" Grid.Column="1" Margin="0,0,0,15" Height="25"/>

        <TextBlock Text="Danh mục:" Grid.Row="1" Grid.Column="0" Margin="0,0,10,15" VerticalAlignment="Center"/>
        <ComboBox x:Name="cboCategory" Grid.Row="1" Grid.Column="1" Margin="0,0,0,15" Height="25" DisplayMemberPath="Name"/>

        <TextBlock Text="Giá bán:" Grid.Row="2" Grid.Column="0" Margin="0,0,10,15" VerticalAlignment="Center"/>
        <TextBox x:Name="txtPrice" Grid.Row="2" Grid.Column="1" Margin="0,0,0,15" Height="25"/>

        <TextBlock Text="Số lượng đầu:" Grid.Row="3" Grid.Column="0" Margin="0,0,10,15" VerticalAlignment="Center"/>
        <TextBox x:Name="txtQuantity" Grid.Row="3" Grid.Column="1" Margin="0,0,0,15" Height="25"/>

        <StackPanel Grid.Row="4" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">
            <Button x:Name="btnSave" Content="Save" Width="80" Margin="0,0,10,0" Click="btnSave_Click"/>
            <Button x:Name="btnCancel" Content="Cancel" Width="80" Click="btnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>