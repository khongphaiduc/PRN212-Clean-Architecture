<Window x:Class="RetailPresentation.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Quản lý bán lẻ" Height="620" Width="1040"
        MinHeight="560" MinWidth="900"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Background="{StaticResource SurfaceBrush}"
                BorderBrush="{StaticResource BorderBrushSoft}"
                BorderThickness="1"
                CornerRadius="8"
                Padding="24"
                Margin="0,0,0,18">
            <Border.Effect>
                <DropShadowEffect BlurRadius="18" ShadowDepth="2" Opacity="0.08" />
            </Border.Effect>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel>
                    <TextBlock Text="Quản lý sản phẩm" Style="{StaticResource PageTitleText}" />
                    <TextBlock Text="Theo dõi danh mục, tồn kho và thao tác nhập hàng"
                               Foreground="{StaticResource MutedTextBrush}"
                               FontSize="14"
                               Margin="0,6,0,0" />
                </StackPanel>

                <StackPanel Grid.Column="1"
                            Orientation="Horizontal"
                            VerticalAlignment="Center">
                    <Button x:Name="btnRefresh"
                            Content="Làm mới"
                            Style="{StaticResource SecondaryButton}"
                            Margin="0,0,10,0"
                            Click="btnRefresh_Click" />
                    <Button x:Name="btnAddProduct"
                            Content="Thêm sản phẩm"
                            Style="{StaticResource ModernButton}"
                            Margin="0,0,10,0"
                            Click="btnAddProduct_Click" />
                    <Button x:Name="btnImportStock"
                            Content="Nhập kho"
                            Style="{StaticResource SuccessButton}"
                            Click="btnImportStock_Click" />
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="1"
                Background="{StaticResource SurfaceBrush}"
                BorderBrush="{StaticResource BorderBrushSoft}"
                BorderThickness="1"
                CornerRadius="8"
                Padding="18"
                Margin="0,0,0,18">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="300" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <TextBlock Text="Danh mục"
                           Style="{StaticResource LabelText}"
                           Margin="0,0,12,0" />
                <ComboBox x:Name="cboCategories"
                          Grid.Column="1"
                          DisplayMemberPath="Name"
                          SelectionChanged="cboCategories_SelectionChanged" />
                <TextBlock Grid.Column="2"
                           Text="Chọn một danh mục để lọc sản phẩm trong bảng"
                           Foreground="{StaticResource MutedTextBrush}"
                           VerticalAlignment="Center"
                           Margin="16,0,0,0" />
            </Grid>
        </Border>

        <Border Grid.Row="2"
                Background="{StaticResource SurfaceBrush}"
                BorderBrush="{StaticResource BorderBrushSoft}"
                BorderThickness="1"
                CornerRadius="8"
                Padding="1">
            <DataGrid x:Name="dgProducts"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      SelectionMode="Single">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="70" />
                    <DataGridTextColumn Header="Tên sản phẩm" Binding="{Binding Name}" Width="2.3*" />
                    <DataGridTextColumn Header="Giá bán" Binding="{Binding Price, StringFormat=N0}" Width="1.2*" />
                    <DataGridTextColumn Header="Tồn kho" Binding="{Binding Quantity}" Width="1*" />
                    <DataGridTextColumn Header="Mã danh mục" Binding="{Binding CategoryId}" Width="1*" />
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</Window>
