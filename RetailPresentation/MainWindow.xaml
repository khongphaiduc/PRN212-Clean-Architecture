<Window x:Class="RetailPresentation.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RetailPresentation"
        mc:Ignorable="d"
        Title="Quản lý Bán lẻ - Danh mục Sản phẩm" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <GroupBox Header="Danh sách Danh mục (Categories)" Grid.Column="0" Margin="0,0,10,0">
            <ListBox x:Name="lstCategories" 
                     DisplayMemberPath="Name" 
                     SelectionChanged="lstCategories_SelectionChanged"/>
        </GroupBox>

        <GroupBox Header="Sản phẩm thuộc Danh mục" Grid.Column="1">
            <DataGrid x:Name="dgProducts" 
                      AutoGenerateColumns="True" 
                      IsReadOnly="True" 
                      SelectionMode="Single"/>
        </GroupBox>
    </Grid>
</Window>